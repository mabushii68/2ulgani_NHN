# AI 활용 및 프롬프트 로그 — 러다이트 2026
> 제출물 #4 「AI 활용 기술 문서」의 **원천 데이터 문서**. 세션마다 갱신되며, D10에 이 문서를 편집·요약하여 최종 PDF를 만든다.
> 갱신 주체: Claude Code (세션 종료 절차의 일부). 사람의 기억 재구성으로 채우는 것 금지 — 실제 사용한 프롬프트 원문만 기록한다.
> 경로: `Assets/_Project/Docs/AI_USAGE_LOG.md`

---

## 0. 기록 규칙 (Claude Code 준수)

1. **세션 종료 시 반드시 append** — §3에 해당 일차 블록이 없으면 만들고, 있으면 세션을 추가한다. 기존 기록 수정·삭제 금지 (오기 정정은 취소선 + 정정 주석)
2. **프롬프트는 원문 그대로** — 요약·윤문 금지. 길면 핵심 지시부만 인용하되 `(…중략…)` 표기
3. 다음 중 하나라도 해당하면 **§2 대표 프롬프트**에도 즉시 복사:
   - AI에게 시스템/아키텍처 구조를 설계·설명시킨 프롬프트
   - AIBrain(회피 예측기) 관련 핵심 지시
   - 결과물이 특히 좋았거나, 실패 → 프롬프트 개선 → 성공의 비교 사례
4. AI 생성 에셋 발생 시 §4에 기록 + **같은 커밋에서 CREDITS.md 갱신** (CLAUDE.md 에셋 규칙)
5. 이 문서에 API 키·토큰·개인정보 절대 기록 금지

---

## 1. 사용 AI 도구 총람

| 도구 | 용도 | 사용 구간 | 비고 |
|---|---|---|---|
| Claude Code (Claude Fable 5) | 코드 작성·리팩터링, Unity MCP 연동, 문서 갱신 | D1~D10 | 세션 규칙: CLAUDE.md |
| Unity MCP | Claude Code ↔ Unity 에디터 연결 (콘솔 조회, 씬/프리팹 조작) | D1~D10 | |
| Claude (chat) | GDD 설계 논의, 대사 풀 생성(P2 착수 시) | 프리프로덕션~ | |
| <!-- 추가 도구 사용 시 행 추가 --> | | | |

---

## 2. 대표 프롬프트 & 구조 설명 (최종 문서 직행 후보)

> 최종 PDF의 "AI 대상 주요 프롬프트 및 지시 사항" 절에 그대로 실을 후보만 엄선. 각 항목은 아래 형식.

### 2.1 [예시 — 첫 기록 시 이 블록을 교체]
- **일차/세션**: D2 세션 1
- **목적**: 회피 예측기(AIBrain) 순수 C# 구현
- **프롬프트 원문**:
  ```
  (여기에 실제 입력한 프롬프트 원문)
  ```
- **결과 및 특기사항**: (AI가 무엇을 산출했고, 사람이 무엇을 수정했는지 1~3줄)
- **선정 이유**: (왜 이 프롬프트가 대표적인가 — 예: 구조 계약을 프롬프트로 강제한 사례)

---

## 3. 일자별 세션 로그

> 형식은 CLAUDE.md의 ai-usage-log 세션 형식과 동일. 하루 블록 아래에 세션을 누적한다.

### D1 — 2026-08-04
#### 세션 1 — 프로젝트 초기 세팅 (문서 체계 + 세션 규칙 확립)
- **목표**: Unity 프로젝트 골격과 제출용 문서 4종을 갖추고, Claude Code 세션 규칙(CLAUDE.md)이 실제 리포 구조와 일치하는지 검증한다. 코드 구현은 착수하지 않음.
- **핵심 프롬프트 (원문)**:
  ```
  CLAUDE.md 읽어
  ```
  ```
  뭔 소리야 다시 읽어봐
  ```
  ```
  G:\내 드라이브\RPG 픽셀 아트 여기에 .zip 파일 제외하고 폴더들이 여러 개
  존재하는데, 3x라고 써져있는 에셋들을 쓸거임. 용도별로 폴더명이 구분되어
  있을텐데, 전부 유니티로 갖고 와서 프리팹화 해서 쓰기 쉽게끔 세팅해주면 좋겠어.
  ```
  ```
  아하 그 나 이제 세션 종료하려고 하는데 며칠차인지는 적을 필요 없고,
  그냥 프로젝트 초기 세팅 했어 오늘은
  ```
- **결과·수정**:
  - 사람 작업: `GDD.md` v1.0 확정(15개 절), 제출용 문서 4종 신규 작성 (`AI_USAGE_LOG.md`, `CREDITS.md`, `SUBMISSION.md`, `TEAM_ROLES_LOG.md`), `CLAUDE.md`·`README.md` 갱신.
  - Claude Code 산출: 문서 전체 정합성 검토. 2건의 불일치 발견 — ① AI 로그 경로가 CLAUDE.md(`ai-usage-log/dayNN.md`)와 실제 문서(`Assets/_Project/Docs/AI_USAGE_LOG.md`) 간 상이 → 후자를 사용하기로 결정. ② `README.md`의 CREDITS 링크가 리포 루트 기준 깨져 있었음 → 실제 경로로 수정. `.gitignore`에 `.idea/` 추가.
  - 첫 프롬프트에서 Claude가 루트만 확인하고 `GDD.md`·AI 로그가 "없다"고 오판 → 사람이 재지시("다시 읽어봐") → `Assets/_Project/Docs/` 확인 후 정정. **교훈: 파일 부재 주장 전 전체 경로 탐색 필수.**
  - 픽셀 아트 에셋(Franuka 18팩, 3x 4355파일 / 179MB) 임포트는 사전 조사만 수행하고 **다음 세션으로 보류**. 조사 결과: 전 팩 48px 그리드 기반, 라이선스는 전부 Franuka 단독 저작·상업 사용 허용·출처 링크 요구(일부 CC BY 4.0). 미결 쟁점 — 임포트 범위(용량), 프리팹 자동 생성 깊이, PPU 확정값, GDD §11(플랫 벡터 도형, 픽셀아트 ❌)과의 아트 방향 충돌.
- **검증**: Unity 콘솔 에러 0 / 경고 0 (코드 미작성 상태). 플레이 모드 스모크·AIBrain 테스트는 대상 기능 없어 해당 없음.
- **커밋 해시**: `a40dc5f`

#### 세션 2 — 최소 전투 (플레이어 이동·조준·홀드 연사 + Main.unity 아레나 골격)
- **목표**: 코드 첫 착수. GDD §3.1의 플레이어 조작(WASD 8방향 / 마우스 조준 / 좌클릭 홀드 자동 연사)과 §2 아레나·고정 카메라를 동작하는 상태로 만든다. 적 FSM·웨이브·AIBrain은 범위 밖.
- **핵심 프롬프트 (원문)**:
  ```
  JungJoon 브랜치 새로 파서 작업 시작할거야. 아직 아무런 작업도 안되어있을거야.
  ```
  이후 Claude Code가 리포 상태를 조사해 선택지를 제시하고, 사람이 2건을 결정:
  ```
  작업 범위 → "최소 전투 (플레이어 이동·조준·연사)"
  입력 시스템 → "구 Input Manager로 전환"
  ```
  (그 외 지시 없음 — 구현 판단은 `CLAUDE.md` + `GDD.md`를 근거로 Claude Code가 자율 수행)
- **결과·수정**:
  - **환경 불일치 4건 발견** (작업 전 리포 조사): ① `activeInputHandler: 1`(신 Input System 전용)인데 CLAUDE.md는 구 Input Manager 요구 → 사람이 `2`(Both)로 변경, 구 `Input.GetAxisRaw` 동작 확인. ② Unity 실제 `6000.3.7f1` vs CLAUDE.md 표기 `6000.0.x`. ③ 패키지에 visualscripting·timeline·multiplayer.center·2d.aseprite/psdimporter 등 미승인 항목 다수. ④ `Assets/_Project/` 하위 폴더가 CLAUDE.md 스펙과 상이(`Art/`·`Data/`·`Input/`·`Settings/`). ①만 처리, ②~④는 사람 판단 대기.
  - Claude Code 산출: 스크립트 7개(`PlayerStatsSO`, `IWeapon`, `IDamageable`, `Projectile`, `ProjectileBlocker`, `BasicWeapon`, `TargetDummy`, `PlayerController`), `PlayerStats_Default.asset`, `Projectile.prefab`, 플레이스홀더 스프라이트 2종(코드 생성 원/사각), `Main.unity`.
  - **실패 → 우회 1**: Unity MCP `execute_code`가 서버 측 BOM 삽입 버그로 전부 컴파일 실패(`Line 1: ﻿`, codedom 백엔드 / Roslyn 미설치). → 일회용 에디터 스크립트(`D1SceneBuilder.cs`)에 `[MenuItem]`을 달아 `execute_menu_item`으로 호출하는 방식으로 우회. 씬 구성을 코드로 결정론적으로 재현할 수 있게 되어 결과적으로 더 나은 경로였고, 사용 후 삭제.
  - **실패 → 수정 2**: `PlayerStatsSO`를 `EditorSceneManager.NewScene` **이전에** 생성해 참조를 넘겼더니, 다른 필드(`_projectilePrefab`·`_aimPivot`)는 정상인데 `_stats`만 조용히 `fileID: 0`으로 직렬화됨. 씬 교체가 애셋 인스턴스 매핑을 무효화한 탓. → SO 확보를 `NewScene` 이후로 옮기고 `SaveAssets()` 후 디스크에서 재로드하도록 수정. **교훈: 에디터 스크립트에서 씬 생성 전후로 얻은 애셋 참조를 섞지 말 것. 참조 누락은 예외 없이 조용히 null이 된다.**
  - 계약 준수 확인: 고정 카메라(Ortho 7.5, 16:9), 단일 씬, 마젠타 미사용(플레이어=전공 파랑 / 적=무채색), 플레이어 넉백 없음(적 넉백만 0.5유닛), 모든 수치 SO 경유.
- **검증**: Unity 콘솔 컴파일 에러 0 / 런타임 에러 0 / 경고 0. 플레이 모드 자동 스모크 3종 — ① 진입 시 참조 누락 에러 없음, ② 3발 × 데미지 10 → HP 30 표적 파괴 로그 확인(발사→비행→트리거 피격→데미지→넉백→파괴 체인), ③ 수명 10초 프로브를 벽으로 발사 → 2초 내 소멸(벽 차단 확인, 수명 만료와 구분됨). **입력 경로(WASD·마우스·홀드 연사)는 MCP로 키보드·마우스를 주입할 수 없어 사람에게 인계 → 김정준이 플레이 모드에서 이동·조준·홀드 연사·표적 파괴 정상 동작 확인.** AIBrain 미변경이라 순수 C# 테스트 해당 없음.
- **커밋 해시**: `34ac874` (브랜치 `JungJoon`)

#### 세션 3 — 환경 정합성 정리 (세션 규칙 문서를 실제 리포에 현행화)
- **목표**: 세션 2에서 발견한 CLAUDE.md ↔ 실제 프로젝트 불일치 5건을 사람이 판단해 확정하고, 문서를 현실 기준으로 갱신한다. 코드 변경 없음.
- **핵심 프롬프트 (원문)**:
  ```
  1. SampleScene.unity에는 아무것도 없어보임. Assets/_Project/Scenes/Main.unity 제대로 있음.
  2. Both 써도 상관 없는거면 걍 냅두고 CLAUDE.md 수정
  3. 일단 보류
  4. 브랜치는 여러개 써도 상관 없는 걸로 수정
  5. Unity 버전은 현재 쓰고 있는 버전으로 표기, 폴더 스펙은 현재 프로젝트 폴더 기준으로 하셈
  ```
- **결과·수정** (사람 결정 → Claude Code 반영):
  1. **빌드 씬 교체**: `Assets/Scenes/SampleScene.unity`(URP 2D 템플릿 기본 — 카메라·Global Light 2D만 있는 빈 씬) → `Assets/_Project/Scenes/Main.unity` 단독. `manage_build`로 변경 후 `File/Save Project`로 `EditorBuildSettings.asset` flush 필요했음 (MCP 호출만으로는 디스크에 반영되지 않음)
  2. **입력**: Active Input Handling `Both` 유지 확정. CLAUDE.md를 "코드는 구 Input Manager API만 사용 / Player Settings는 Both / `InputSystem_Actions.inputactions`·`UnityEngine.InputSystem` API 사용 금지"로 개정 — 원래 규칙의 **의도**(입력 6개에 신 시스템은 과잉)는 유지하고 **수단**만 현실에 맞춤
  3. **아트 방향 보류**: 픽셀아트(Franuka 18팩) vs GDD §11 플랫 벡터 도형 — 미결 유지. 현재 플레이스홀더가 플랫 도형이므로 사실상 GDD 방향으로 진행 중
  4. **브랜치 전략 개정**: main 단일 → 담당자별 작업 브랜치 + main 병합. `CLAUDE.md` 커밋 절 + `TEAM_ROLES_LOG.md` §4 갱신
  5. **버전·폴더 현행화**: `Unity 6 LTS (6000.0.x)` → `6000.3.7f1` (CLAUDE.md + CREDITS.md §6). 폴더 스펙을 실제 구조로 재작성하고, 용도 미확정 4개(`Art/`·`Data/`·`Input/`·`Settings/`)를 **사용 금지 + 사람 확정 필요**로 명시. CREDITS.md 위치 표기도 `저장소 루트` → 실제 위치 `Assets/_Project/Docs/`로 정정
  - 패키지 항목은 사람 지시에 없었으나 같은 성격의 불일치여서 함께 처리: "TMP·URP 외 없음"(사실과 다름) → "현재 manifest 집합을 기준선으로, 추가는 승인 필수 / 미사용 패키지 제거는 GUID·템플릿 의존성 위험으로 하지 않고 D8 WebGL 실측 후 판단". 제약의 의도는 보존.
  - 세션 2의 교훈을 규칙으로 승격: 세션 종료 조건 2번에 "키보드·마우스 입력은 MCP 주입 불가 → 임시 `[MenuItem]` 스모크로 최대한 자동 검증 + 남는 입력 경로는 사람 확인 후 커밋" 추가
- **검증**: 코드 변경 없음. Unity 콘솔 에러 0 / 경고 0. `EditorBuildSettings.asset`에 `Main.unity` 단독 등록 확인.
- **커밋 해시**: `1b20120` (브랜치 `JungJoon`)

<!-- D2 ~ D10 블록은 해당 일차 첫 세션 종료 시 생성 -->

---

## 4. AI 생성 에셋 기록

> 비게임플레이 요소만 허용 (CLAUDE.md). CREDITS.md와 이중 기록.

| 일차 | 에셋 | 도구 | 프롬프트 원문 | 사용 위치 | CREDITS.md 반영 커밋 |
|---|---|---|---|---|---|
| | | | | | |

---

## 5. 실패·폐기 기록 (문서 가치 있음 — 삭제 금지)

> 시도했으나 폐기한 AI 활용 (예: §14.1 ML-Agents 착수 후 폐기). 최종 문서에 "시도와 판단" 서사로 활용.

| 일차 | 시도 내용 | 폐기 사유 | 배운 점 |
|---|---|---|---|
| | | | |

---

## 6. D10 최종 문서 변환 체크리스트

- [ ] §1 도구 총람 → 최종 문서 "사용 AI 도구" 절
- [ ] §2 대표 프롬프트 → "주요 프롬프트 및 지시 사항" 절 (5~8개 엄선)
- [ ] AIBrain 구조 설명(온라인 조건부 확률 모델) + ML-Agents 탑재 여부에 따른 2단 구성 판단 (GDD §14.1)
- [ ] §4 + CREDITS.md → "외부 에셋 / 오픈소스 출처" 절 (누락 시 실격 리스크)
- [ ] §5 실패 기록 중 서사 가치 있는 것 포함 여부 판단
- [ ] PDF 변환 및 링크·이미지 확인
